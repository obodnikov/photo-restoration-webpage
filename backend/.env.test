# Test Environment Configuration
#
# This file IS committed to version control because it contains only test credentials.
# It ensures consistent test environment across all developers and CI/CD.
# DO NOT put real secrets here - only use fake/test values.

# Application Settings
APP_ENV=testing
APP_NAME="Photo Restoration API - Test"
APP_VERSION="1.0.0-test"
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# CORS - Test origins
CORS_ORIGINS=["http://localhost:3000","http://testserver"]

# Security - FIXED VALUES FOR DETERMINISTIC TESTING
# IMPORTANT: These are test-only values. Never use in production!
# SECRET_KEY must be at least 32 characters
SECRET_KEY=test_secret_key_for_deterministic_jwt_tokens_min_32_characters_long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Authentication (Test credentials)
# Password must be short enough for bcrypt (max 72 bytes)
AUTH_USERNAME=testuser
AUTH_PASSWORD=testpass

# HuggingFace API (Mock in tests)
HF_API_KEY=test_hf_api_key_12345
HF_API_TIMEOUT=60
HF_API_URL=https://api-inference.huggingface.co/models

# Models Configuration (Test config with real model IDs for compatibility)
# Using real model IDs so existing tests pass, but these won't be called in tests (mocked)
MODELS_CONFIG=[{"id":"swin2sr-2x","name":"Swin2SR 2x Upscale","model":"caidas/swin2SR-classical-sr-x2-64","category":"upscale","description":"Fast 2x upscaling","parameters":{"scale":2}},{"id":"swin2sr-4x","name":"Swin2SR 4x Upscale","model":"caidas/swin2SR-classical-sr-x4-64","category":"upscale","description":"Fast 4x upscaling","parameters":{"scale":4}},{"id":"qwen-edit","name":"Qwen Image Enhance","model":"Qwen/Qwen-Image-Edit-2509","category":"enhance","description":"AI-powered enhancement and restoration","parameters":{"prompt":"enhance details, remove noise and artifacts"}}]

# Database - In-memory for tests
DATABASE_URL=sqlite+aiosqlite:///:memory:

# File Storage - Temporary test directories
UPLOAD_DIR=./test_data/uploads
PROCESSED_DIR=./test_data/processed
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=[".jpg",".jpeg",".png"]

# Session Management
SESSION_CLEANUP_HOURS=24
